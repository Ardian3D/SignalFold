import type { Incident, IncidentUpdate } from '@/features/incidents/domain/incidentTypes';
export type DashboardOverview = { activeIncidentsCount: number; sev1Sev2Active: number; openTasks: null; taskDataAvailable: false; resolvedThisWeek: number; averageTimeToAcknowledge: number|null; averageTimeToResolve: number|null; activeIncidents: Incident[]; needsAttention: Incident[]; recentActivity: IncidentUpdate[]; recentIncidents: Incident[]; serviceSummary: { operational: number; degraded: number; outage: number; maintenance: number }; quickCreateCapability: boolean; demoWorkspaceState: { isDemo: boolean; canSeed: boolean } };
