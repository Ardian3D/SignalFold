export const json=(b:Record<string,unknown>,s=200)=>Response.json(b,{status:s});export const clean=(v:unknown,m=5000)=>typeof v==='string'?v.trim().slice(0,m):'';export const requestId=(v:unknown)=>{const i=clean(v,128);return/^[A-Za-z0-9_-]{8,128}$/.test(i)?i:null};export async function authorize(b:any,o:unknown,admin=false){const u=await b.auth.me();if(!u)throw{code:'UNAUTHENTICATED',status:401};const id=clean(o,128);const ms=await b.asServiceRole.entities.Membership.filter({organization_id:id,user_id:u.id,status:'active'});if(!ms[0]||(admin&&ms[0].role!=='admin'))throw{code:admin?'FORBIDDEN':'NOT_A_MEMBER',status:403};return{user:u,membership:ms[0],organizationId:id}}export const failure=(e:unknown)=>{const x=e as{code?:string;status?:number};return json({error:x.code??'UNKNOWN'},x.status??500)};
