import { createClientFromRequest } from 'npm:@base44/sdk'; import { authorize, failure, json, requestId, slugify } from './operations.ts';
const services=[['Checkout Web','high'],['Payments API','critical'],['Order Processor','high'],['Customer Portal','medium']];
const incidents=[['Checkout payments failing after latest deployment','Customers cannot complete card payments after the latest deployment. Support has received 37 reports in the last 12 minutes.','SEV1','reported'],['Order processing delays exceed normal thresholds','Order processing latency is elevated and requires active operational investigation.','SEV2','investigating'],['Customer portal login latency recovered','Customer portal latency returned to normal after a safe configuration rollback.','SEV3','resolved'],['Intermittent webhook delivery delay','A historical low-priority webhook delivery delay was monitored and closed.','SEV4','closed']];
Deno.serve(async req=>{try{const b=createClientFromRequest(req),i=await req.json(),a=await authorize(b,i.sourceOrganizationId,true),rid=requestId(i.requestId);if(i.confirmation!=='CREATE DEMO WORKSPACE'||!rid)throw{code:'DEMO_CONFIRMATION_REQUIRED',status:400};const memberships=await b.asServiceRole.entities.Membership.filter({user_id:a.user.id,status:'active'});let demoOrg:any=null;for(const m of memberships){const o=await b.asServiceRole.entities.Organization.get(m.organization_id);if(o?.is_demo&&o?.created_by_user_id===a.user.id){demoOrg=o;break;}}if(!demoOrg){const collisions=await b.asServiceRole.entities.Organization.filter({slug:'northstar-commerce-demo'});demoOrg=await b.asServiceRole.entities.Organization.create({name:'Northstar Commerce',slug:collisions.length?`northstar-commerce-demo-${collisions.length+1}`:'northstar-commerce-demo',default_timezone:'UTC',incident_prefix:'SF',public_status_enabled:false,created_by_user_id:a.user.id,is_demo:true,settings:{use_case:'demo'}});await b.asServiceRole.entities.Membership.create({organization_id:demoOrg.id,user_id:a.user.id,role:'admin',status:'active',joined_at:new Date().toISOString(),display_title:'Demo workspace owner'});}let existingServices=await b.asServiceRole.entities.Service.filter({organization_id:demoOrg.id,is_demo:true});for(const [name,criticality] of services){if(!existingServices.some((s:any)=>s.name===name))await b.asServiceRole.entities.Service.create({organization_id:demoOrg.id,name,slug:slugify(name),criticality,operational_status:'operational',tags:[],is_active:true,is_demo:true,request_id:`demo-service-${slugify(name)}`});}existingServices=await b.asServiceRole.entities.Service.filter({organization_id:demoOrg.id,is_demo:true});const payment=existingServices.find((s:any)=>s.name==='Payments API');const existingIncidents=await b.asServiceRole.entities.Incident.filter({organization_id:demoOrg.id,is_demo:true});let created=0;for(let n=0;n<incidents.length;n++){const [title,description,severity,status]=incidents[n];if(existingIncidents.some((x:any)=>x.title===title))continue;const now=new Date(Date.now()-n*86400000).toISOString(),incident=await b.asServiceRole.entities.Incident.create({organization_id:demoOrg.id,code:`SF-${new Date().getUTCFullYear()}-${String(42+n).padStart(4,'0')}`,title,description,source:'demo',service_id:n===0?payment?.id:undefined,reporter_user_id:a.user.id,severity,severity_source:'rule_baseline',status,reported_at:now,resolved_at:status==='resolved'?now:undefined,closed_at:status==='closed'?now:undefined,recovery_verified:status==='resolved'||status==='closed',public_visibility:'private',is_demo:true,reopened_count:0,request_id:`demo-incident-${n}`});await b.asServiceRole.entities.IncidentUpdate.create({organization_id:demoOrg.id,incident_id:incident.id,event_type:status==='resolved'?'incident_resolved':status==='closed'?'incident_closed':'incident_seeded',actor_type:'system',visibility:'internal',message:`Demo incident ${incident.code} seeded.`,metadata:{code:incident.code,status,severity},occurred_at:now,is_demo:true});created++;}await b.asServiceRole.entities.User.update(a.user.id,{default_organization_id:demoOrg.id});return json({organizationId:demoOrg.id,created,reconciled:created===0});}catch(e){return failure(e);}});
