export const json=(b:Record<string,unknown>,s=200)=>Response.json(b,{status:s});export const clean=(v:unknown,m=5000)=>typeof v==='string'?v.trim().slice(0,m):'';export const slugify=(v:string)=>v.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'').slice(0,80)||'service';export const requestId=(v:unknown)=>{const i=clean(v,128);return/^[A-Za-z0-9_-]{8,128}$/.test(i)?i:null};export async function authorize(b:any,o:unknown,admin=false){const u=await b.auth.me();if(!u)throw{code:'UNAUTHENTICATED',status:401};const id=clean(o,128);if(!id)throw{code:'VALIDATION_FAILED',status:400};const ms=await b.asServiceRole.entities.Membership.filter({organization_id:id,user_id:u.id,status:'active'}),m=ms[0];if(!m)throw{code:'NOT_A_MEMBER',status:403};if(admin&&m.role!=='admin')throw{code:'FORBIDDEN',status:403};return{user:u,membership:m,organizationId:id}}export const failure=(e:unknown)=>{const x=e as{code?:string;status?:number};return json({error:x.code??'UNKNOWN'},x.status??500)};export const safeService=(r:any)=>({id:r.id,organization_id:r.organization_id,name:r.name,slug:r.slug,description:r.description,criticality:r.criticality,operational_status:r.operational_status,owner_user_id:r.owner_user_id,tags:r.tags??[],is_active:r.is_active,is_demo:r.is_demo,created_date:r.created_date,updated_date:r.updated_date});
